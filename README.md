# slowapi
NOTE: for now, end-to-end test is the main.py file, which will create a API server in FastAPI style. Type uvicorn main:app to run the server.

TODOS:
- test DI system for:
    - unnamed callables(like object of fastapi.security.OAuth2PasswordBearer class)
